In order to meet the objective to build an understandable 
step by step tutorial, the kmeans algorithm is introduced 
first using animations. Those animations are created in the 
second part of the tutorial and saved as gifs. To display
everything correctly in the html file, the gifs are already
included.